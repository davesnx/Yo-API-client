import Base from './api/common/Base';
import Valid from './api/common/Valid';

class JustYo {

    constructor (apiToken) {
        this.url = 'https://api.justyo.co/';
        this.apiToken = apiToken;
    }
}
